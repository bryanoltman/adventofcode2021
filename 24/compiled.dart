Iterable<int> programInput(int number) sync* {
  final digits = number.toString().split('').map(int.parse);
  for (final digit in digits) yield digit;
}

main() {
  final input = programInput(1234).iterator;
  int w = 0;
  int x = 0;
  int y = 0;
  int z = 0;

  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 1;
  x += 11;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 6;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 1;
  x += 13;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 14;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 1;
  x += 15;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 14;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 26;
  x += -8;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 10;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 1;
  x += 13;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 9;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 1;
  x += 15;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 12;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 26;
  x += -11;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 8;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 26;
  x += -4;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 13;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 26;
  x += -15;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 12;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 1;
  x += 14;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 6;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 1;
  x += 14;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 9;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 26;
  x += -1;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 15;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 26;
  x += -8;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 4;
  y *= x;
  z += y;
  input.moveNext();
  w = input.current;
  x *= 0;
  x += z;
  x %= 26;
  z ~/= 26;
  x += -14;
  x = x == w ? 1 : 0;
  x = x == 0 ? 1 : 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 10;
  y *= x;
  z += y;
}
